package day4;

public class IfCondition {
    public static void main(String[] args) {
        int age = 20;

        if (age > 21) {
            System.out.println(age + " is eligible for vote"); // no output
        }
        // else {
        // System.out.println(age + " is not eligible for vote");
        // }
    }
}
